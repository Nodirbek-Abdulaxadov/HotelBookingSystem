namespace Entities
{
    public enum OrderStatus
    {
        Unknown,
        Waiting,
        Confirmed,
        Active,
        Completed
    }

    public enum RoomStatus
    {
        Unknown,
        Busy,
        Empty,
        Ordered
    }
}
