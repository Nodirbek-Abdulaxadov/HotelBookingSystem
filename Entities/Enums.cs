namespace Entities
{
    public enum OrderStatus
    {
        Unknown,
        Waiting,
        Confirmed,
        Active,
        Completed
    }

    public enum RoomStatus
    {
        Unknown,
        Empty,
        Busy,
        Ordered
    }
}
