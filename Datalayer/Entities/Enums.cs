namespace Datalayer.Entities
{
    public enum OrderStatus
    {
        Waiting,
        Confirmed,
        Active,
        Completed
    }

    public enum RoomStatus
    {
        Busy,
        Empty,
        Ordered
    }
}
